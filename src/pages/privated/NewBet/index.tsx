import { PrivatedScreen } from "@components";

import * as S from "./styles";

export const NewBet = () => {
  return (
    <PrivatedScreen
      navButtons={[
        { text: "Home", path: "/" },
        { text: "Account", path: "/" },
      ]}
    >
      dsad
    </PrivatedScreen>
  );
};
